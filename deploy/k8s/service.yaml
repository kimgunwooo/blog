apiVersion: v1
kind: Service
metadata:
  name: blog
  labels:
    app.kubernetes.io/name: blog
    app.kubernetes.io/part-of: blog
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: blog
  ports:
    - name: http
      port: 4321
      targetPort: http
